import { ForNumberDirective } from './fornumber.directive';

describe('ForNumberDirective', () => {
  it('should create an instance', () => {
    const directive = new ForNumberDirective(null, null);
    expect(directive).toBeTruthy();
  });
});
